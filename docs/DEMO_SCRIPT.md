# Final competition film — 1:59 target

Selected product name: **MCPilot Adaptive Interactive Play Lab**.

The film has one job: make a judge see that **the player's visible decisions change which WebMCP tools exist**. Start inside the working product, show one chronological example, and put real agent tool calls—not an app-rendered counter—at the center. Do not type live. Remove only latency and dead air. Mark shortened play footage clearly.

## Locked cut

| Time | Picture and action | Narration | Edit rule |
|---|---|---|---|
| **0:00–0:08** | Start already inside the human baseline. Show the beginning and finish with one obvious jump cut labeled **20-SECOND RUN · SHORTENED**. Hold the completed **PLAYED TRIAL** state. | “This player runs a short course once, creating device-local, source-labeled evidence—not a diagnosis.” | No logo or title screen. Keep enough continuous play to make human control obvious. |
| **0:08–0:12** | The prepared request is already in chat. Press **Send**; do not type. | “The request is already pasted. I press Send.” | A visible jump cut may remove agent latency. |
| **0:12–0:30** | Show the actual agent calls to `inspect_play_lab`, `read_play_signals`, and `propose_access_tune`. Keep tool names and real outputs readable. | “The browser agent calls three real WebMCP tools: it inspects the live lab, reads the played signals, and proposes the smallest reversible tune that preserves the challenge.” | Tool calls are the centerpiece. Do not replace them with mock chat or an app-only animation. |
| **0:30–0:44** | Show the exact proposal rows, unchanged settings, six-tool inventory, and Apply absent. The player clicks **Approve exact plan** once. Keep approval → `apply_approved_tune` registration continuous. | “The exact before-and-after values are visible, but Apply does not exist. Only the player can approve. Their click creates `apply_approved_tune` as tool seven.” | This is the only approval moment in the film. No cut from click through registration. |
| **0:44–0:54** | Send the prepared apply request and show the actual `apply_approved_tune` call. Show reviewed values activate, Apply disappear, and Undo appear. | “The agent applies only that approved revision, once. Apply disappears, and exact undo takes its place.” | A latency cut may follow Send; keep the tool call and state transition continuous. |
| **0:54–1:04** | Show the same player's adapted trial with the same clearly marked shortening treatment. Hold the completed run. | “The same player tests the reviewed settings on the same course. The agent never plays.” | Attribute both runs to the same consenting player. |
| **1:04–1:14** | Show Compare absent. The player gives the uncoached visible check-in. Keep click → `compare_play_trials` registration continuous. | “Comparison is still absent. The player gives an uncoached check-in, and only then does `compare_play_trials` appear.” | Do not coach the response or cut the capability transition. |
| **1:14–1:36** | Send the prepared comparison request. Show the actual call and the real result board: both sources, four deltas, player response, regressions, and exact verdict. | “That tool returns the real, source-linked result shown here. In this run, [EXACT HEADLINE DELTA]. It shows every material regression and the player's response. The verdict is [EXACT VERDICT].” | Use whatever the run proves. No victory cue for a mixed or negative result. |
| **1:36–1:47** | Send the prepared undo request. Show the actual `undo_last_tune` call, all prior values restored, Undo removed, and completed evidence retained. | “Finally, exact undo restores the prior settings without deleting the completed evidence.” | Keep the call and restoration continuous. |
| **1:47–1:57** | Hold the real post-undo capability surface and agent inventory. | “This is imperative WebMCP registration inside a React page: deterministic code owns bounds, provenance, verdicts, and undo; the agent never receives approval or check-in tools.” | Let the real interface carry the implementation proof. |
| **1:57–1:59** | Minimal **MCPilot Adaptive Interactive Play Lab** and tagline lockup over the real interface. | No narration. | End cleanly. |

## Prepared requests

Paste these before recording each clip. Only the Send click belongs in the film.

**After baseline**

> Inspect my baseline and tune the game with the smallest reversible change that preserves the challenge.

**After visible approval**

> Apply the exact plan the player just approved.

**After the visible player check-in**

> Compare this adapted trial with the player's baseline. Report every available measured delta and any material regression, and respect the player's check-in.

**After comparison**

> Undo that tune and verify the prior settings are restored.

## Truth rules

- Both trials must be completed by the same consenting person. Fictional sample data is never human-outcome evidence.
- The player selects the check-in without coaching. Never choose **Better for me** for presentation value.
- Narration and captions must use the exact visible metrics and verdict from the recorded take.
- Do not claim diagnosis, broad accessibility, or player benefit beyond the recorded evidence.
- Do not splice approval, check-in, the start of any tool call, or the resulting capability transition.

## Export gate

- [ ] Runtime is preferably `1:58–2:05` and strictly below `3:00`; the locked cut targets `1:59`.
- [ ] Working product appears in the first second; the first real WebMCP call begins by `0:12`.
- [ ] Actual tool calls occupy the center of the story, with one approval example and no repeated climax.
- [ ] Shortened play is labeled; all waiting and live typing are removed.
- [ ] Narration explains both what was built and how imperative WebMCP registration is used.
- [ ] Captions, audio, exact result, signed-out playback, and media rights all pass final review.
